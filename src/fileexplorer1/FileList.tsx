import React, { useRef, useEffect } from 'react';
import * as monaco from 'monaco-editor';

const FileList = () => {
    return (
        <div id="file-list">
            
        </div>
    );
}

export default FileList;